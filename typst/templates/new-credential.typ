// ============================================================
// LumaCV template: Credential
// Purpose: Credential/education-forward structure for regulated, certified, or senior specialist roles.
// ============================================================
#import "/support/resume-core.typ": render-resume

#let render-credential(data, variant: "default", theme: "credential") = render-resume(data, mode: "credential")
#let render = render-credential
