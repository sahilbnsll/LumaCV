// ============================================================
// LumaCV template: Switch
// Purpose: Career-switcher format that surfaces transferable capabilities before chronology.
// ============================================================
#import "/support/resume-core.typ": render-resume

#let render-switch(data, variant: "default", theme: "switch") = render-resume(data, mode: "switch")
#let render = render-switch
