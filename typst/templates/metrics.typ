// ============================================================
// LumaCV template: Metrics
// Purpose: Metrics-led header with selected outcomes placed before the career chronology.
// ============================================================
#import "/support/resume-core.typ": render-resume

#let render-metrics(data, variant: "default", theme: "metrics") = render-resume(data, mode: "metrics")
#let render = render-metrics
