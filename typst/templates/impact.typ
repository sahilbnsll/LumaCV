// ============================================================
// LumaCV template: Impact
// Purpose: Achievement-first resume with a bold results hierarchy; best for senior ICs and performance-heavy roles.
// ============================================================
#import "/support/resume-core.typ": render-resume

#let render-impact(data, variant: "default", theme: "impact") = render-resume(data, mode: "impact")
#let render = render-impact
